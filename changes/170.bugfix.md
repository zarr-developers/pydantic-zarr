Fixed `model_like` and `BaseModel.like` reporting a spurious mismatch when two specs have JSON-identical attributes that differ only by lists versus tuples (for example a freshly built spec compared against the same spec read back from storage, where `from_zarr` converts attribute lists to tuples). Comparison now uses a recursive, list/tuple-insensitive equality check (`json_eq`).
